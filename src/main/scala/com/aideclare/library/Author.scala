package com.aideclare.library

import com.aideclare.entities.dsl.syntax.{id, password, username}
import com.aideclare.entities.dsl.typeclasses.{EntityMetadata, UserInfo}

import java.time.LocalDate

case class Author(
                   @id(autogenerated = true) id: Int,
                   @username username: String,
                   @password password: String,
                   name: String,
                   birthDate: LocalDate) derives EntityMetadata, UserInfo
